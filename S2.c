#include <stdio.h>

int main(int argc, char const *argv[])
{
    int c ; 
    int count = 0;
    while((c = getchar())!= EOF ){
        if (c == 'a' || c == 'e') {
            count++ ;
        }
        
        /* code */
    printf("there is %d of A and B",count);

    }
        printf("WWE ARE HERE");
    return 0;
}
